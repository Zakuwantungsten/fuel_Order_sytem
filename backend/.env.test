# Test Environment Configuration
# This file contains test-specific secrets and should NOT be committed to version control
# Use this file for local testing only

# Node Environment
NODE_ENV=test

# MongoDB
MONGODB_URI=mongodb://localhost:27017/fuel-order-test

# JWT Secrets (Test-only values - never use in production)
JWT_SECRET=test-jwt-secret-length-must-be-sufficient-for-testing-purposes-only-not-production-safe
JWT_REFRESH_SECRET=test-refresh-secret-length-must-be-sufficient-for-testing-purposes-only-not-production-safe
JWT_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# Logging
LOG_LEVEL=info
LOG_FILE=/tmp/test-app.log

# Port (Use different port for tests)
PORT=5001

# CORS
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Rate Limiting (Relaxed for tests)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# Encryption Keys (Test-only)
BACKUP_ENCRYPTION_KEY=test-backup-encryption-key-must-be-at-least-12-chars-long-for-testing
FIELD_ENCRYPTION_KEY=test-field-encryption-key-must-be-at-least-12-chars-long-for-testing

# R2 Configuration (Optional for tests)
R2_ENDPOINT=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=fuel-order-test-backups

# Email Configuration (Test defaults)
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=test@example.com
EMAIL_PASSWORD=test-password
EMAIL_FROM=test@fuelorder.local
EMAIL_FROM_NAME=Fuel Order Test

# System Timezone
TZ=Africa/Nairobi
