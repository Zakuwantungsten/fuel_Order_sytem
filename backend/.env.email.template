# ============================================
# SMTP EMAIL CONFIGURATION (Optional)
# ============================================
# Configure these to enable email notifications
# If not configured, system will work but won't send emails

# SMTP Host (e.g., Gmail, Outlook, Yahoo)
SMTP_HOST=smtp.gmail.com

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Use SSL/TLS (true for port 465, false for port 587)
SMTP_SECURE=false

# Email account for sending notifications
SMTP_USER=your-email@gmail.com

# Email password or app password
# For Gmail: Generate App Password from Google Account settings
# For Outlook: Use account password or app password
# For Yahoo: Use app password (Account Settings → Security)
SMTP_PASS=your-app-password

# ============================================
# EXAMPLES FOR DIFFERENT PROVIDERS
# ============================================

# GMAIL (Recommended):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=yourname@gmail.com
# SMTP_PASS=xxxx-xxxx-xxxx-xxxx  (16-char App Password)
#
# How to get Gmail App Password:
# 1. Go to Google Account → Security
# 2. Enable 2-Step Verification
# 3. Search "App passwords"
# 4. Select "Mail" and generate password
# 5. Copy 16-character password to SMTP_PASS

# OUTLOOK/OFFICE 365:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=yourname@outlook.com
# SMTP_PASS=your-password

# YAHOO:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=yourname@yahoo.com
# SMTP_PASS=your-app-password
#
# How to get Yahoo App Password:
# 1. Go to Account Info → Security
# 2. Enable Two-Step Verification
# 3. Generate app password for "Other App"
# 4. Use that password in SMTP_PASS

# CUSTOM SMTP SERVER:
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=admin@yourdomain.com
# SMTP_PASS=your-password

# ============================================
# NOTES
# ============================================
# - After adding these variables, restart backend server
# - Test email in Super Admin Dashboard → Security Tab
# - If email fails, check backend logs in backend/logs/
# - System works WITHOUT email config (just no notifications)
